{
  "resources": {
    "includes": [
      {
        "pattern": "com/sun/jna/.*/(libjnidispatch\\.so|jnidispatch\\.dll)$",
        "comment": "JNA's bundled native helper, packed at com/sun/jna/<arch>/. Without this entry native-image strips them and Keyring.create() fails with UnsatisfiedLinkError on every platform."
      },
      {
        "pattern": "^osxkeychain\\.so$",
        "comment": "java-keyring 1.0.4 ships its macOS Keychain JNI helper at the JAR root as osxkeychain.so (not under com/github/javakeyring). The static path keeps native-image from stripping it during the resource scan."
      },
      {
        "pattern": "^examples/recipes/.*/recipe\\.json$",
        "comment": "SSO-2873 — bundled example recipes the RecipeInterpreter reads from the classpath at runtime (examples/recipes/<id>/recipe.json). native-image strips un-referenced resources, so list them explicitly. JSON (not YAML) is shipped so the runtime needs no YAML parser."
      },
      {
        "pattern": "^examples/recipes/.*/provision\\.(ya?ml|json)$",
        "comment": "SSO-3100 — a bundled recipe's provisioning file (examples/recipes/<id>/provision.yaml), referenced by the recipe's `provision` key and converged by `examples apply` before the steps run. Read from the classpath next to recipe.json; native-image strips un-referenced resources, so list it explicitly."
      },
      {
        "pattern": "^examples/recipe\\.schema\\.json$",
        "comment": "SSO-2872 — the recipe schema, bundled for reference/validation."
      },
      {
        "pattern": "^examples/connection\\.schema\\.json$",
        "comment": "SSO-2948 — the connection contract schema, bundled beside the recipe schema for reference/validation. Referenced by ConnectionSchemaConformanceTest; native-image strips un-referenced resources so it must be listed explicitly."
      },
      {
        "pattern": "^provision/provision\\.schema\\.json$",
        "comment": "SSO-3088 — the provisioning-file schema (.thoryn/provision.yaml), bundled beside the recipe + connection schemas for reference/validation. native-image strips un-referenced resources, so list it explicitly."
      },
      {
        "pattern": "^version\\.properties$",
        "comment": "SSO-2953 — the build-stamped version resource read at runtime by VersionProvider to answer `thoryn --version`. native-image strips un-referenced resources, so list it explicitly or the binary reports 'unknown'."
      }
    ]
  }
}
