# Maven build output
target/
!**/src/main/**/target/
!**/src/test/**/target/
HELP.md
.flattened-pom.xml

# Maven wrapper (only-script): keep mvnw / mvnw.cmd / maven-wrapper.properties;
# the downloaded distribution lives under ~/.m2/wrapper and is never committed.

# SSO-3088 — provisioning receipts (`thoryn provision apply` writes <name>.receipt.json next to the
# file; secret-free, but per-account state like Terraform state — not committed by default).
.thoryn/*.receipt.json

# SSO-3119 — `--secret-file` output. `provision apply` / `clients create` write a one-time client
# secret to the path you name, and the natural place to type is the repo you are standing in. A
# credential must never reach a commit, so the extension is ignored outright.
*.secret

# GraalVM native-image build artifacts
*.build_artifacts.txt
/thoryn
/thoryn.exe

### Python (recipe/native-image tooling) ###
__pycache__/
*.pyc

### IntelliJ IDEA ###
.idea
*.iws
*.iml
*.ipr

### Eclipse / STS ###
.apt_generated
.classpath
.factorypath
.project
.settings
.springBeans
.sts4-cache

### VS Code ###
.vscode/

### macOS ###
.DS_Store

### OS package / distribution scratch ###
/dist/
/nbdist/
