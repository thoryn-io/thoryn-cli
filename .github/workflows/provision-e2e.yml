name: provision-e2e

# SSO-3090 (epic SSO-3087) — demo caller for the reusable provisioning Action
# (.github/actions/provision). thoryn-cli DOGFOODS its own provisioning-as-code: the Action signs in
# from the committed connection contract (`.thoryn/connection.json`, SSO-2951) and converges the
# committed provisioning file (`.thoryn/provision.yaml`) with `thoryn provision apply` (SSO-3088/3089)
# against SHARED STAGING, then `thoryn provision destroy`s it on exit. Real product APIs only.
#
# The ONLY secret this workflow injects is `THORYN_CLI_CI_CLIENT_SECRET` (the env var the contract's
# `auth.secretEnv` names) — the client id, workspace, scopes and the desired resources are all in the
# committed `.thoryn/` files.
#
# The file provisions a SANDBOX environment (`cli-ci`) inside the `thoryn` workspace plus a loopback
# OAuth client in it; destroy hard-deletes the sandbox (cascading the client). A leftover from a failed
# run is ADOPTED by its slug on the next apply, never duplicated (see `.thoryn/provision.yaml`).
#
# Manual only (workflow_dispatch): it mutates a shared-staging workspace and needs the secret, so it
# must not fire on push/PR. Requires a `cli-v*` release carrying `thoryn provision apply`
# (cli-v0.11.0+) — pin one with the cli-version input until it is the latest.
#
# ONE-TIME OPERATOR SETUP (see `.thoryn/README.md` "One-time bootstrap"): a founder mints the CI
# machine client once (it is declared in the provisioning file; `thoryn provision apply --secret-file`
# delivers its one-time secret, SSO-3113), pastes its clientId into
# `.thoryn/connection.json`, and sets the `THORYN_CLI_CI_CLIENT_SECRET` repository secret.

on:
  workflow_dispatch:
    inputs:
      hub:
        description: "Hub base URL to provision against (supplied to the CLI as THORYN_HUB)."
        required: false
        default: "https://hub.stg.thoryn.org"
      cli-version:
        description: "cli-v* release tag to run (default: latest). Needs cli-v0.11.0 or newer."
        required: false
        default: ""
      destroy:
        description: "Remove what the run created on exit (\"true\"/\"false\"). The sandbox itself is a long-lived fixture and is never removed."
        required: false
        default: "true"
      foreign-environment-id:
        description: "Id of an environment in the workspace that the CI identity must NOT reach (e.g. the workspace's production environment). When set, the run asserts a GET of it answers 404. Leave empty to assert list confinement only."
        required: false
        default: ""

# Serialize: never let two provisioning runs race on the shared workspace (the file's sandbox slug is fixed).
concurrency:
  group: provision-e2e
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  provision:
    runs-on: ubuntu-latest
    steps:
      - name: Check out thoryn-cli (the .thoryn/ files)
        uses: actions/checkout@v4

      - name: Provision as a real customer (sign in from contract + provision apply + destroy)
        id: provision
        uses: ./.github/actions/provision
        with:
          # The ONLY injected secret — the env var the connection contract's auth.secretEnv names.
          client-secret: ${{ secrets.THORYN_CLI_CI_CLIENT_SECRET }}
          hub: ${{ inputs.hub }}
          cli-version: ${{ inputs.cli-version }}
          destroy: ${{ inputs.destroy }}

      # SSO-3113 (epic SSO-3108) — prove the CI identity is CONFINED, not merely that it can do its job.
      # The Action leaves the downloaded thoryn.jar and the signed-in session in the workspace.
      #  1. Reads are confined (ADR 2026-09-15 §8): as cli-ci, `env list` must return exactly the sandbox
      #     this identity manages. A workspace-wide identity would list the production environment too.
      #  2. A foreign environment answers 404 — only meaningful when its EXISTENCE is known, because by
      #     design a foreign environment and a nonexistent one are the same 404 from cli-ci's side. So the
      #     id comes from a dispatch input set by someone who can see it, never a guessed or random id.
      #     This is a GET, not a DELETE: if confinement were ever broken, a destructive probe would
      #     destroy a real environment instead of failing the run.
      - name: Assert the CI identity is confined (SSO-3113)
        env:
          FOREIGN_ENVIRONMENT_ID: ${{ inputs.foreign-environment-id }}
          EXPECTED_SANDBOX: ${{ steps.provision.outputs.environment }}
        run: |
          set -euo pipefail
          listed=$(java -jar thoryn.jar env list --output json | jq -r '(.data // .)[] | .slug' | sort)
          echo "Environments visible to the CI identity:"; echo "$listed" | sed 's/^/  /'
          if [ "$listed" != "$EXPECTED_SANDBOX" ]; then
            echo "::error::confinement broken — expected the CI identity to see only '$EXPECTED_SANDBOX', saw: $(echo "$listed" | tr '\n' ' ')" >&2
            exit 1
          fi
          if [ -z "$FOREIGN_ENVIRONMENT_ID" ]; then
            echo "::notice::no foreign-environment-id supplied — list confinement asserted; foreign GET skipped."
            exit 0
          fi
          set +e
          body=$(java -jar thoryn.jar env get "$FOREIGN_ENVIRONMENT_ID" --output json)
          rc=$?
          set -e
          status=$(echo "$body" | jq -r '.httpStatus // empty' 2>/dev/null || true)
          if [ "$rc" -eq 0 ] || [ "$status" != "404" ]; then
            echo "::error::confinement broken — GET of foreign environment answered rc=$rc httpStatus=${status:-none}, expected 404" >&2
            echo "$body" >&2
            exit 1
          fi
          echo "Foreign environment answered 404 — the CI identity cannot reach it."

      # Downstream consumers use the Action's outputs. The destroy inside the Action runs on exit, so
      # the sandbox + client ids are only valid for the life of the job (the workspace + issuer persist).
      - name: Show provisioned identifiers
        run: |
          echo "workspace-slug = ${{ steps.provision.outputs.workspace-slug }}"
          echo "environment    = ${{ steps.provision.outputs.environment }}"
          echo "client-id      = ${{ steps.provision.outputs.client-id }}"
          echo "issuer         = ${{ steps.provision.outputs.issuer }}"
          echo "receipt        = ${{ steps.provision.outputs.receipt }}"
